class numVowels{
    public static void main(String[] args){
        // Check that there is exactly 1 command line argument (the input word for analysis).
        if (args.length != 1){
            System.out.println("Usage: java numVowels <\"word\">");
            return;
        }

        // Get the word from the command line argument.
        String word = args[0];

        // METHOD countVowels(word)

            // look through the word character by character.

            // set up a counter variable (set to 0).

            // increment the counter for every vowel found.

            // once the end of the word has been reached, 
            // stop searching
            // print the number of vowels found.

        //EXIT method
    }
}